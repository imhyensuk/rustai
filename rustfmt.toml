# Wide-ish lines and generous small-block heuristics: this codebase leans on
# short guard clauses and single-expression matches, and forcing those onto
# four lines each hurts more than it helps.
max_width = 100
use_small_heuristics = "Max"
newline_style = "Unix"
